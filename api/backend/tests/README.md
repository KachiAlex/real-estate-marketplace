Phase 4 API test suite

Run these tests locally after installing dev dependencies:

```bash
npm install --save-dev jest supertest
npm run test:phase4
```

Scripts added to project-level package.json:

- `test:phase4` — runs Jest using `backend/tests/jest.phase4.config.js`
